# Local-only sync config for this machine
canon-sync.local.json

# Local-only canon/context imports. These stay available in the workspace
# but must never be committed to the public website repo.
canon/
source-assets/

# Raw source content folders (numbered "NN - name") — kept in the workspace
# for authoring, promoted into the site via scripts/promote-site-asset.ps1.
[0-9][0-9] - */

# OS / editor noise
desktop.ini
Thumbs.db
.DS_Store
